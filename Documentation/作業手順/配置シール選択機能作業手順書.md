# 配置シール選択機能 作業手順書

## 目的
- シール配置フェーズ中に、画面左下へ所持シール一覧のスクロール UI を表示する。
- 一覧から選択したシールを、配置プレビューと実際の配置対象へ反映する。
- `StickerPeeling` フェーズでは一覧を非表示にし、`StickerPlacement` 再入時は未選択状態へ戻す。
- 所持シール一覧は Inspector の固定配列ではなく、所持品管理データを参照する構造で追加する。

## 完成イメージ
- 起動直後の `StickerPlacement` フェーズで、左下に所持シール一覧パネルが表示される。
- 一覧セルには画像が表示され、先頭シールが初期選択状態になっている。
- 一覧内の別シールをクリックまたはタップすると、選択ハイライトと配置プレビューが切り替わる。
- 選択中シールがある時だけ、ワールド上へそのシールを配置できる。
- `StickerPeeling` フェーズへ遷移すると一覧は非表示になる。
- `StickerPlacement` に戻ると選択が解除され、再度選ぶまで配置できない。

## 作成・更新するもの
- 新規: `Assets/Scripts/StickerSelection/OwnedStickerDefinition.cs`
- 新規: `Assets/Scripts/StickerSelection/OwnedStickerInventorySource.cs`
- 新規: `Assets/Scripts/StickerSelection/StickerSelectionState.cs`
- 更新: `Assets/Scripts/TapStickerPlacer.cs`
- 更新: `Assets/Scripts/HudScreenBinder.cs`
- 更新: `Assets/Scripts/Phase/SealPhaseController.cs`
- 更新: `Assets/UI/UXML/HudScreen.uxml`
- 更新: `Assets/UI/USS/HudScreen.uss`
- 更新: `Assets/Main.unity`

## 事前確認
1. Unity でプロジェクトを開く。
2. シーン [Main.unity](/Users/tatsuki/Projects/Unity/SealFairy/Assets/Main.unity) を開く。
3. [配置シール選択機能要件書.md](/Users/tatsuki/Projects/Unity/SealFairy/Documentation/要件書/配置シール選択機能要件書.md) と [plan.md](/Users/tatsuki/Projects/Unity/SealFairy/Documentation/plan.md) を開き、今回の前提を確認する。
4. [TapStickerPlacer.cs](/Users/tatsuki/Projects/Unity/SealFairy/Assets/Scripts/TapStickerPlacer.cs)、[HudScreenBinder.cs](/Users/tatsuki/Projects/Unity/SealFairy/Assets/Scripts/HudScreenBinder.cs)、[HudScreen.uxml](/Users/tatsuki/Projects/Unity/SealFairy/Assets/UI/UXML/HudScreen.uxml)、[HudScreen.uss](/Users/tatsuki/Projects/Unity/SealFairy/Assets/UI/USS/HudScreen.uss) の現在実装を確認する。
5. 今回は既存構成を維持しながら、選択状態と左下一覧 UI を追加する最小差分で進める。

## 手順1: シール選択用のデータ定義を追加する
1. `Assets/Scripts/StickerSelection` フォルダを作成する。
2. `OwnedStickerDefinition.cs` を新規作成する。
3. 一覧表示と配置に必要な最低限の定義を持たせる。
4. 実装イメージは以下。

```csharp
using UnityEngine;

[System.Serializable]
public sealed class OwnedStickerDefinition
{
    [SerializeField] private string id;
    [SerializeField] private Sprite icon;
    [SerializeField] private PeelSticker3D stickerPrefab;

    public string Id => id;
    public Sprite Icon => icon;
    public PeelSticker3D StickerPrefab => stickerPrefab;
}
```

5. 一覧セルの表示内容は画像のみを前提にし、名称ラベルは今回追加しない。
6. 配置に使う参照は `PeelSticker3D` prefab に統一する。

## 手順2: 所持品管理データの取得口を追加する
1. `OwnedStickerInventorySource.cs` を新規作成する。
2. 役割は「所持品管理データから現在の所持シール一覧を返す窓口」に限定する。
3. 将来の本実装を差し替えやすくするため、UI や配置クラスから具体的な保存形式を隠す。
4. 実装イメージは以下。

```csharp
using System.Collections.Generic;
using UnityEngine;

public sealed class OwnedStickerInventorySource : MonoBehaviour
{
    [SerializeField] private List<OwnedStickerDefinition> ownedStickers = new();

    public IReadOnlyList<OwnedStickerDefinition> GetOwnedStickers()
    {
        return ownedStickers;
    }
}
```

5. 実際の所持品管理データが別クラスにある場合は、このクラスの内部で変換して返す。
6. 仮実装の配列を置く場合でも、呼び出し側はこのクラスだけを見るようにする。

## 手順3: 選択状態管理クラスを追加する
1. `StickerSelectionState.cs` を新規作成する。
2. 役割は所持シール一覧、選択中シール、未選択状態の一元管理にする。
3. 起動直後の初期選択と、フェーズ再入時の選択解除を分けて扱える API を用意する。
4. 実装イメージは以下。

```csharp
using System.Collections.Generic;

public sealed class StickerSelectionState
{
    public IReadOnlyList<OwnedStickerDefinition> OwnedStickers { get; private set; }
    public OwnedStickerDefinition SelectedSticker { get; private set; }

    public void SetOwnedStickers(IReadOnlyList<OwnedStickerDefinition> ownedStickers)
    {
        OwnedStickers = ownedStickers;
    }

    public void SelectInitialSticker()
    {
        SelectedSticker = OwnedStickers != null && OwnedStickers.Count > 0 ? OwnedStickers[0] : null;
    }

    public void Select(OwnedStickerDefinition sticker)
    {
        SelectedSticker = sticker;
    }

    public void ClearSelection()
    {
        SelectedSticker = null;
    }
}
```

5. 同じセルを再度押した場合も未選択へ戻さないため、`Select` は常に上書きだけでよい。
6. 所持シール 0 件時は `SelectedSticker` を `null` にする。

## 手順4: UXML に左下一覧を追加する
1. [HudScreen.uxml](/Users/tatsuki/Projects/Unity/SealFairy/Assets/UI/UXML/HudScreen.uxml) を開く。
2. 既存の `top-bar` と `bottom-right-menu` は維持したまま、左下パネルを追加する。
3. `root` 直下に `bottom-left-sticker-panel` を追加し、その中へ `sticker-scroll-view` を配置する。
4. 最低限の構造例は以下。

```xml
<ui:VisualElement name="bottom-left-sticker-panel">
    <ui:Label name="sticker-list-title" text="シール一覧" />
    <ui:ScrollView name="sticker-scroll-view" mode="Vertical" />
</ui:VisualElement>
```

5. 一覧セル自体は C# で動的生成するため、UXML へ固定で複数セルを書かない。
6. 空状態を表示する場合は `empty-sticker-list-label` を追加してもよい。

## 手順5: USS に左下一覧スタイルを追加する
1. [HudScreen.uss](/Users/tatsuki/Projects/Unity/SealFairy/Assets/UI/USS/HudScreen.uss) を開く。
2. 左下パネル、スクロール領域、画像セル、選択ハイライト用の style を追加する。
3. 左下パネルはワイヤー準拠の大きめ矩形として固定配置する。
4. 追加方針は以下。
   - `#bottom-left-sticker-panel`: `position: absolute`, `left`, `bottom`, `width`, `height`
   - `#sticker-scroll-view`: パネル内で縦スクロール
   - `.sticker-cell`: セル全体の余白、背景、角丸
   - `.sticker-cell--selected`: 選択中ハイライト
   - `.sticker-cell__image`: 画像表示サイズ固定
5. 画像セルは押しやすいサイズを確保し、パネル高さを超えたらスクロールできることを優先する。
6. 既存右下メニューと重ならない位置関係を Game ビューで確認しながら調整する。

## 手順6: HudScreenBinder に一覧生成と選択制御を追加する
1. [HudScreenBinder.cs](/Users/tatsuki/Projects/Unity/SealFairy/Assets/Scripts/HudScreenBinder.cs) を開く。
2. `UIDocument` から `sticker-scroll-view` と左下パネル root を取得できるようにする。
3. `OwnedStickerInventorySource` と `StickerSelectionState` を参照できるよう、SerializeField か初期化経路を追加する。
4. 起動時に所持シール一覧を読み込み、セルを動的生成する。
5. 初回だけ `SelectInitialSticker()` を呼び、UI ハイライトを更新する。
6. セル押下時の処理は次の順で行う。
   - `selectionState.Select(sticker)`
   - セルの選択見た目更新
   - `TapStickerPlacer` または共有状態へ選択変更を反映
7. `PhaseChanged` 受信時は以下の動作にする。
   - `StickerPlacement`: 左下一覧を表示する
   - `StickerPeeling`: 左下一覧を非表示にする
8. `StickerPlacement` へ戻ったタイミングでは `selectionState.ClearSelection()` を呼び、見た目も未選択状態へ戻す。
9. 起動直後の初期選択と、フェーズ再入時の選択解除を混同しないよう、初回初期化フラグを分ける。

## 手順7: TapStickerPlacer を選択中シール参照へ変更する
1. [TapStickerPlacer.cs](/Users/tatsuki/Projects/Unity/SealFairy/Assets/Scripts/TapStickerPlacer.cs) を開く。
2. 固定の `templateSticker` を前提にしている箇所を整理し、選択中シールから prefab を取得するよう変更する。
3. 追加方針は以下。
   - `StickerSelectionState` 参照を持つ
   - `SelectedSticker == null` の場合は配置しない
   - 選択変更時にプレビュー用インスタンスを差し替える
4. `Update()` 冒頭のガード例は以下。

```csharp
if (!Application.isPlaying || !isPlacementEnabled)
{
    return;
}

if (selectionState.SelectedSticker == null)
{
    return;
}

if (IsPointerOverUi())
{
    return;
}
```

5. `CacheTemplateSticker()` は `FindTemplateSource()` 依存から、選択中 `StickerPrefab` を基準にしたキャッシュへ置き換える。
6. `SpawnSticker()` は選択中シールの prefab を元に生成し、既存どおり妖精判定と `StickerRuntimeRegistry.Register()` を呼ぶ。
7. 選択解除時はプレビューも非表示または破棄して、未選択状態が見た目で分かるようにする。

## 手順8: UI 操作中の誤配置を防ぐ
1. `TapStickerPlacer` に UI 上ポインタ判定を追加する。
2. UI Toolkit だけで完結しない場合は `EventSystem.current.IsPointerOverGameObject()` など利用可能な経路を確認する。
3. 少なくとも一覧セル押下時に、同フレームで `SpawnSticker()` が呼ばれないようにする。
4. 左下一覧だけでなく既存 `ready-button` など他 UI 上の押下でも配置しない動作に揃える。

## 手順9: フェーズ再入時の選択解除を接続する
1. [SealPhaseController.cs](/Users/tatsuki/Projects/Unity/SealFairy/Assets/Scripts/Phase/SealPhaseController.cs) を開く。
2. `StickerPlacement` への再入タイミングを `HudScreenBinder` 側で判定できるなら、既存 `PhaseChanged` 通知だけで処理してよい。
3. それで不足する場合だけ、再入時初期化用の明示的な通知を追加する。
4. 完了条件は「起動直後は先頭選択」「フェーズ再入時は未選択」の 2 パターンが両立していること。

## 手順10: Main.unity の参照を設定する
1. [Main.unity](/Users/tatsuki/Projects/Unity/SealFairy/Assets/Main.unity) を開く。
2. `HudScreenBinder` を持つ GameObject に `OwnedStickerInventorySource` の参照を設定する。
3. 必要なら `TapStickerPlacer` にも `StickerSelectionState` を共有するための参照経路を設定する。
4. 所持シール一覧に使う画像と prefab の対応が Inspector 上で確認できることを確認する。
5. 所持品管理データ実体が別にある場合は、そのデータ参照が `OwnedStickerInventorySource` に正しく接続されていることを確認する。

## 手順11: 動作確認
1. Play モードへ入る。
2. 起動直後の `StickerPlacement` で左下に一覧が表示され、先頭シールが選択済みであることを確認する。
3. 一覧内の別シールを押すと、選択ハイライトとプレビュー画像が切り替わることを確認する。
4. 選択中の状態でワールドをクリックまたはタップし、選択したシールが配置されることを確認する。
5. 一覧セルや右上ボタンの上を押しても、ワールドへシールが誤配置されないことを確認する。
6. `StickerPeeling` へ遷移すると左下一覧が消えることを確認する。
7. `StickerPlacement` に戻ると選択が解除され、未選択のままでは配置されないことを確認する。
8. 再度一覧からシールを選ぶと配置を再開できることを確認する。
9. 所持シール 0 件のケースでも、パネル表示が破綻せず空状態が分かることを確認する。

## 完了条件
- 左下に所持シール一覧のスクロール UI が追加されている。
- 一覧セルには画像が表示され、任意のシールを選択できる。
- 選択中シールがプレビューと配置結果へ反映される。
- `StickerPeeling` フェーズでは一覧が非表示になる。
- `StickerPlacement` 再入時は未選択状態に戻り、選択するまで配置されない。
- UI 操作中にワールドへ誤ってシールが配置されない。
- 所持シール一覧の取得口が所持品管理データ接続前提の構造になっている。

## 補足
- 所持品管理データの本実装がまだ未整備なら、仮データを `OwnedStickerInventorySource` に閉じ込めておき、`HudScreenBinder` と `TapStickerPlacer` からは直接参照しない。
- 画像表示は今回必須要件だが、画像のアスペクト比が揃っていない場合は CSS 側でトリミング方針を決めておくと UI 崩れを防ぎやすい。
- 初期選択と再入時未選択は意図が異なるため、`OnEnable` のような単一タイミングへまとめず、初回初期化とフェーズイベントを分けて扱う。
